import styled from "styled-components";

const Title = styled.h1`
  font-size: max(8rem, 3.5rem);
  letter-spacing: -2px;
  text-align: center;
  font-weight: bolder;
`;

const SubTitle = styled.h2`
  font-size: max(4rem, 2rem);
  font-weight: bold;
  letter-spacing: -2px;
  text-align: center;
`

export {Title, SubTitle};